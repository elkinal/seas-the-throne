package edu.cornell.jade.seasthethrone;

/** Variables specifying settings about the build. */
public class BuildConfig {
  /** If the code should be build with debug information. */
  public static final boolean DEBUG = true;
}
